# VPS Environment Variables for 185.201.8.177
# Ready to deploy configuration

# Database Configuration (Using existing PostgreSQL on VPS)
POSTGRES_DB=timesheet
POSTGRES_USER=apiuser
POSTGRES_PASSWORD=Devo0932
POSTGRES_HOST=172.19.0.4  # PostgreSQL container IP in Docker network

# Backend Configuration
SECRET_KEY=your_production_secret_key_that_matches_server_12541
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=480
REFRESH_TOKEN_EXPIRE_DAYS=30
ENVIRONMENT=production

# VPS Configuration
VPS_IP=185.201.8.177
BACKEND_PORT=8130
FRONTEND_PORT=3020

# Domain Configuration
DOMAIN=timesheet-api.aerocars.cc
EMAIL=admin@example.com
SSL_EMAIL=admin@example.com

# Admin Configuration (for initial setup)
ADMIN_PASSWORD=Admin123!

# Frontend API Configuration
VITE_API_BASE_URL=https://timesheet-api.aerocars.cc/api/v1

# Notes:
# - PostgreSQL is already running on port 5432 (container: my_postgres_db)
# - Backend will run on port 8130 (to avoid conflicts with 8000, 8110, 8120)
# - Frontend will run on port 3020 (to avoid conflict with goodconn on 3010)
# - Using existing PostgreSQL database instead of creating new container